{
  "name": "blackrim-nimbus-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Marketing + docs site for blackrim-nimbus-skills: the cloud-toolkits marketplace and its GasCity hosting pack. Built on the voidzero toolchain (Vite, Vitest).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.8",
    "@fontsource-variable/jetbrains-mono": "^5.2.8"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
